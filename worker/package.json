{
  "name": "hok-codex-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute hok-codex-db --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute hok-codex-db --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.90.0",
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5"
  }
}
